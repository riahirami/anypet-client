export type RootState = {
    category: CategoryState;
    auth: AuthState;
    ad: AdState;
    favorite: FavoriteState;
    notification: NotificationState;
  };
  
  export type CategoryState = {
    // Define category state properties here
  };
  
  export type AuthState = {
    // Define auth state properties here
  };
  
  export type AdState = {
    // Define ad state properties here
  };
  
  export type FavoriteState = {
    // Define favorite state properties here
  };
  
  export type NotificationState = {
    // Define notification state properties here
  };
  
  // Add other state types for additional state slices
  
  
  